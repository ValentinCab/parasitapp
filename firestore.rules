rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Requiere inicio de sesión. No usar reglas abiertas en una publicación pública.
    match /atlas/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
